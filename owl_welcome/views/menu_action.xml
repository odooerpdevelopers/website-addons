<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Client Action (Patrón Real owl_examples) -->
        <record id="action_owl_welcome_component" model="ir.actions.client">
            <field name="name">OWL Welcome Action</field>
            <field name="tag">owl_welcome_component</field>
            <field name="target">current</field>
        </record>

        <!-- Menú en Contacts (Familiar para todos) -->
        <menuitem
            id="menu_owl_welcome"
            name="🚀 OWL Welcome"
            parent="contacts.menu_contacts"
            action="action_owl_welcome_component"
            sequence="10" />
    </data>
</odoo>